<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="Teal">#5FA3B3</Color>
    <Color x:Key="TealBright">#8ECCDB</Color>
    <Color x:Key="TealLight">#B8E6F2</Color>
    <Color x:Key="TealDark">#2A5C6E</Color>
    <Color x:Key="TealGlow">#555FA3B3</Color>
    <Color x:Key="BgSurface">#F7080808</Color>
    <Color x:Key="TextPrimary">#F8FFFFFF</Color>
    <Color x:Key="TextSecondary">#CCF0FFFF</Color>
    <Color x:Key="TextDim">#775FA3B3</Color>

    <SolidColorBrush x:Key="TealBrush" Color="{StaticResource Teal}" />
    <SolidColorBrush x:Key="TealBrightBrush" Color="{StaticResource TealBright}" />
    <SolidColorBrush x:Key="TealLightBrush" Color="{StaticResource TealLight}" />
    <SolidColorBrush x:Key="TealDarkBrush" Color="{StaticResource TealDark}" />
    <SolidColorBrush x:Key="TealGlowBrush" Color="{StaticResource TealGlow}" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}" />
    <SolidColorBrush x:Key="TextDimBrush" Color="{StaticResource TextDim}" />

    <FontFamily x:Key="UIFont">pack://application:,,,/Assets/Fonts/#JetBrains Mono</FontFamily>

    <DropShadowEffect x:Key="GlowTeal" ShadowDepth="0" Color="#5FA3B3" BlurRadius="14" Opacity="0.4" />
    <DropShadowEffect x:Key="GlowTealStrong" ShadowDepth="0" Color="#5FA3B3" BlurRadius="24" Opacity="0.5" />
    <DropShadowEffect x:Key="GlowTealSoft" ShadowDepth="0" Color="#5FA3B3" BlurRadius="6" Opacity="0.2" />

    <Style x:Key="HudButton" TargetType="Button">
        <Setter Property="FontFamily" Value="{StaticResource UIFont}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Foreground" Value="{StaticResource TextDimBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="0"
                            BorderThickness="0,0,0,1"
                            BorderBrush="#115FA3B3"
                            Padding="16,10"
                            RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1" />
                        </Border.RenderTransform>
                        <TextBlock Text="{TemplateBinding Content}"
                                   FontFamily="{StaticResource UIFont}"
                                   FontSize="14"
                                   FontWeight="Normal"
                                   Foreground="{TemplateBinding Foreground}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        To="#445FA3B3" Duration="0:0:0.15" />
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).ScaleX"
                                                         To="1.04" Duration="0:0:0.12" />
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).ScaleY"
                                                         To="1.04" Duration="0:0:0.12" />
                                        <ColorAnimation Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)"
                                                        To="#8ECCDB" Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                        To="#115FA3B3" Duration="0:0:0.2" />
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).ScaleX"
                                                         To="1.0" Duration="0:0:0.2" />
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).ScaleY"
                                                         To="1.0" Duration="0:0:0.2" />
                                        <ColorAnimation Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)"
                                                        To="#775FA3B3" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource TealLightBrush}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
                            <Setter Property="BorderBrush" Value="{StaticResource TealGlowBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Scanlines" TargetType="Rectangle">
        <Setter Property="Fill">
            <Setter.Value>
                <DrawingBrush TileMode="Tile" Viewport="0,0,1,3"
                              ViewportUnits="Absolute"
                              Viewbox="0,0,1,3" ViewboxUnits="Absolute">
                    <DrawingBrush.Drawing>
                        <DrawingGroup>
                            <GeometryDrawing Geometry="M0,0 L1,0 L1,1 L0,1Z" Brush="#08000000" />
                            <GeometryDrawing Geometry="M0,1 L1,1 L1,2 L0,2Z" Brush="#03000000" />
                            <GeometryDrawing Geometry="M0,2 L1,2 L1,3 L0,3Z" Brush="#00000000" />
                        </DrawingGroup>
                    </DrawingBrush.Drawing>
                </DrawingBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Opacity" Value="0.15" />
        <Setter Property="IsHitTestVisible" Value="False" />
    </Style>

    <Style x:Key="Vignette" TargetType="Rectangle">
        <Setter Property="Fill">
            <Setter.Value>
                <RadialGradientBrush>
                    <RadialGradientBrush.RelativeTransform>
                        <TransformGroup>
                            <ScaleTransform ScaleX="1.8" ScaleY="1.8" CenterX="0.5" CenterY="0.5" />
                        </TransformGroup>
                    </RadialGradientBrush.RelativeTransform>
                    <GradientStop Color="#00000000" Offset="0.5" />
                    <GradientStop Color="#88000000" Offset="1" />
                </RadialGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="IsHitTestVisible" Value="False" />
    </Style>

</ResourceDictionary>
